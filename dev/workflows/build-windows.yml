name: Windows Installer Build

on:
  push:
    branches: [main]
    tags:
      - 'v*'
    paths:
      - 'core/**'
      - 'platform/windows/**'
      - '.github/workflows/build-windows.yml'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-installer:
    runs-on: windows-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install Inno Setup
        run: choco install innosetup --no-progress -y
        shell: cmd

      - name: Build installer
        working-directory: platform/windows
        run: iscc "MyBookshelf.iss"
        shell: cmd

      - name: Get version
        id: ver
        run: |
          $v = (Select-String '#define MyAppVersion\s+"(.+)"' platform/windows/MyBookshelf.iss).Matches[0].Groups[1].Value
          echo "version=$v" >> $env:GITHUB_OUTPUT
        shell: pwsh

      # Edge/IE는 .exe 직접 다운로드를 차단할 수 있어 ZIP도 함께 제공한다.
      - name: Zip installer (browser-download fallback)
        run: Compress-Archive -Path dist/windows/Setup.exe -DestinationPath dist/windows/MyBookshelf-Setup.zip -Force
        shell: pwsh

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: MyBookshelf-Setup-v${{ steps.ver.outputs.version }}
          path: |
            dist/windows/Setup.exe
            dist/windows/MyBookshelf-Setup.zip
          retention-days: 30

      - name: Create Release (on tag push)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/windows/Setup.exe
            dist/windows/MyBookshelf-Setup.zip
          generate_release_notes: true
