# My Bookshelf v1.3.0 — 화면 정리

## 반영

- 7칸 내비 → flex 아이콘 레일. 현재 단계만 이름 표시. 전체 이름은 말풍선·키보드 포커스·aria-label로 제공. 작업 중 링크 잠금과 직접 쿼리 이동 차단.
- 상태 지표 4개 → 회색 칩 한 줄. 현재 작업의 설정 모델과 Wiki 개수 표시. 키 등록 여부를 연결 성공으로 표현하지 않음. AI 미설정·선택한 공급자 비활성·번역 모델 오류 경고는 크게 유지.
- 머리줄의 글자 크기 ± 제거. 설정의 ‘화면과 저장 위치’로 85–135% 조절을 이동하고 저장. 중첩 요소에서 글자가 거듭 확대되는 CSS도 수정.
- 전체 저장 위치는 설정으로 이동. 화면별 폴더 열기는 유지하되 펼쳤을 때 세로 배치. 항목별 열기/보기는 아이콘화하면서 접근성 이름을 유지.
- 최소 창 380×600(추가 요청 반영), 신규 설정의 첫 실행 폭 1100. 정상 종료 시 마지막 크기를 저장하고 다음 실행에서 현재 화면에 맞게 제한. 작은 화면에서는 화면 안에 들어오는 크기를 우선함.
- 진행 중 실패 항목·현재 문서 실패 문단 수를 별도 표시(0은 회색, 실패 시 붉은색). 종료 후 실패 목록, 미번역 문단 수, 재시도 버튼 유지.
- 설정 저장은 잠금·원자적 쓰기를 사용하여 다른 설정이나 API 키를 덮어 잃지 않게 보강.

## 설명 분류

| 처리 | 대상 | 이유 |
|---|---|---|
| 화면 유지 | 외부 AI 전송·민감정보 안내 | 인지하지 못한 전송 방지 |
| 화면 유지 | 짧은 문서·장 구조 미감지 선택 안내 | 분할/단일장/삭제 결정을 돕는 내용 |
| 화면 유지 | 업로드는 대기열 등록이며 시작 버튼이 필요하다는 안내 | 업로드만으로 처리됐다고 오인하지 않도록 함 |
| 화면 유지 | 번역 실패, OCR 검증 한계·불량본 경고, 결과 언어 변경, 기존 파일 이동 안 됨 | 누락·손실·잘못된 완료 판단 방지 |
| 도움말 이동 | 단계 소개, 메뉴 카드 부연, 드래그앤드롭, 논문 URL 입력 방법 | 반복되는 사용법 |
| 도움말 이동 | 설정 항목 설명, 폴더 경로 입력 설명, UI 언어 설명, 모델 목록 출처 | 해당 조작 옆에서 찾아 읽을 수 있음 |
| 삭제 | 상단 반복 작업 흐름, 긴 앱 소개 중 제목/메뉴와 중복인 부분 | 동일 정보의 반복 |
| 설정에 유지 | 저작권 상세 문단 | 상시 노출 대신 기존 접이식 안내 유지 |

## 검증 범위

- 비교 기준은 사용자 안의 v1.2.80이 아니라 실제 직전 설치본 v1.2.82.
- 소스 AST 기준 columns 호출 52→46, caption 호출 57→46. 화면 픽셀 실측이나 화면별 동시 표시 개수가 아님.
- 메뉴·전체실행·설정·변환·분할·번역·요약·출력 × 한국어/영어 = 16회 AppTest 렌더.
- 빈 화면의 표시 문구에 한글이 남는 영어 화면 검사, 내비 접근성 이름, 모델 선택 저장, 폰트 저장, 실제 목록의 아이콘 버튼 이름, 실패 재시도 검사.
- 창 크기 기본값·복원·작은 화면·손상 설정 보정 및 설정 저장 동시성 검사.
- 전체 unittest 304개 통과, compileall 및 diff 공백 검사 통과.
- Browser 스킬로 연결을 시도했으나 사용 가능한 브라우저가 없었음. 따라서 760×600/1100px에서 실제 스크린샷·가로 넘침 실측, 실제 화면 낭독기 및 Windows 실기 검증은 미완료. AppTest는 브라우저 픽셀 레이아웃 검사가 아님.

기존 HWP/HWPX 글상자 추출, 번역 부분완료 보호, 모델 선택 기능은 유지한다. 원본 문서 재처리·변경과 공개 배포는 하지 않는다.

## 적용 상태

- v1.3.0 빌드: `dist/.mac-build.noindex/MyBookshelf.app`.
- Mac 번들 표시와 Windows 설치 버전 모두 1.3.0.
- 최초 설치 시 문서 처리 자식 프로세스를 발견해 교체를 보류했으며, 사용자 작업 종료 확인 후 2026-09-08 13:24(KST)에 설치·재시작 완료.
- `/Applications/MyBookshelf.app` 코드와 Mac 번들 버전 모두 v1.3.0. 검증한 소스/의존성 목록 45개 파일과 설치본 해시 일치.
- 서버 상태 확인 정상(`ok`), 새 앱 프로세스 기동 및 재시작 이후 신규 오류 로그 없음 확인.
- 기존 앱 백업: `~/Library/Application Support/MyBookshelf/backups/20260908-before-v1.3.0/MyBookshelf.app`.
- 문서·사용자 설정은 변경하지 않음. 760px 실제 화면 검증은 별도 필요.

## 380px 추가 조정 (2026-09-08)

- 버전은 사용자 요청대로 1.3.0 유지.
- 560px 이하에서 CSS로 자동 전환: 본문 기본 13px, 보조문구 11.5px, 제목 16px. 설정의 85–135% 배율은 그대로 적용. 버튼·체크박스 클릭 영역 최소 40px 유지.
- 상단 내비는 사용자 후속 요청에 따라 기존 SVG 그림 아이콘 7개 유지(접이식 메뉴 철회). 좁은 창에서는 7칸 그리드로 한 줄 배치하고 현재 단계 이름만 시각적으로 숨김. 선택 강조·말풍선·접근성 이름·키보드 포커스·작업 중 이동 잠금 유지. 넓은 창에서는 기존처럼 현재 단계 이름도 표시.
- 문서 목록은 체크박스와 제목을 첫 줄에, 보기/수정 버튼을 다음 줄에 배치. 설정과 일반 다중 칸은 한 열로 전환. 파일 업로드·메뉴 카드·긴 경로도 좁은 폭에 맞춤. 실패/보안/선택 경고는 숨기지 않음.
- 장 편집은 ‘한 장씩 편집’을 기본값으로 제공하고 ‘표로 편집’ 선택 가능. 편집 방식은 창 폭과 별개로 사용자가 선택. 장/방식 전환 시 임시 수정 유지, 원본 변경 시 낡은 편집 상태 초기화, 합치기 선택 시 삭제 경고 표시. 기존 저장/확정 처리 경로를 재사용.
- 최소 창 폭 380px. 설치 시 이번 창 크기만 380px로 조정하고 종전 높이를 유지하며, 이후 정상 종료 시 마지막 크기를 기억.
- unittest 311개 통과, 한국어/영어 16뷰 렌더 및 장 편집·모드 전환 추가 검사. 표의 직접 셀 입력은 AppTest 미지원이므로 위젯 반환값을 모의하여 상태 보존 검사. compileall/diff 검사 통과.
- Browser 연결 목록이 비어 있어 실제 380px/확대 배율별 가로 넘침·스크린샷 검증은 미완료. Windows 실기 역시 미검증.
- 수정본 설치·재시작 완료. 설치 소스/의존성 46개 파일 해시 일치, 번들 버전 v1.3.0, 로컬 서버 health `ok`, 재시작 후 신규 오류 로그 0개 확인.
- 이번 창 설정은 종전 760×739에서 380×739로 변경. 문서와 AI 모델/키 설정은 건드리지 않음.
- 직전 앱 백업: `~/Library/Application Support/MyBookshelf/backups/20260908-before-380/MyBookshelf.app`. 최초 1.3.0 설치 전 백업도 그대로 보존.
- 아이콘 메뉴 복원 및 Streamlit 1.62의 stLayoutWrapper 대응까지 최종 설치 완료. 311개 테스트 재통과, 설치본 46개 파일 일치 및 서버 정상 응답 재확인. 중간 수정본도 `backups/20260908-before-380-css-check/MyBookshelf.app`에 보존.

## 사용자 스크린샷에 따른 글자 크기·정렬 조정

- 380px 화면의 앱 이름 16→20px, 버전 약 6.7→10.5px. 메뉴·구역 제목 16→13.5px, 접이식 항목 및 본문 품질 검사 버튼 12px, 업로드 용량·확장자 안내 10px. 사용자 폰트 배율은 계속 적용.
- 메뉴명 아래 폴더 열기에 6px 추가 여백. 좁은 화면의 구분선 위아래 과도한 여백 축소.
- EPUB/DOCX/HWPX/Obsidian은 창 폭과 관계없이 세로 행으로 나열. 각 행을 독립된 2칸 그리드로 만들어 폴더 버튼을 오른쪽 40px 칸에 고정하고 접근성 이름도 부여.
- 보기 아이콘은 눈 대신 문서 모양으로 변경. 보기 버튼은 파일명 시작선에 맞춰 다음 줄 왼쪽에 고정. 수정 버튼이 있는 경우 겹치지 않게 별도 칸 유지.
- Wiki 완료의 폴더 버튼에 ‘Wiki 저장 폴더 열기’ 이름 표시. Obsidian 앱 열기와 폴더 열기의 구분 유지.
- 전체 테스트 314개 통과. 형식별 폴더 버튼의 실제 대상 경로 전달, Wiki 완료 버튼 이름, 문서 보기 아이콘, 한/영 전뷰 렌더 검사. compileall/diff 검사 통과.
- 사용자 제공 두 스크린샷을 기준으로 수정했으며 수정 후 실제 픽셀 화면 캡처 검사는 미완료.
- 적용 완료: 설치본 46개 파일 일치, 버전 v1.3.0 유지, 재시작 후 서버 health `ok` 및 신규 오류 로그 0개 확인. 기존 앱은 `backups/20260908-before-spacing/MyBookshelf.app`에 보존. 이번 작업에서 문서·모델·창 크기 설정은 변경하지 않음.

## 책별 대기목록 접기·들여쓰기

- 번역·요약 등 책별 대기목록의 챕터를 20px 들여쓰기 컨테이너 안의 접이식 목록으로 변경. 기본은 접힘. 책 체크박스와 선택한 챕터 수는 밖에 표시.
- 책 전체 선택/해제와 개별 챕터 선택 동기화 유지. 접혀 있어도 챕터 위젯을 렌더하여 선택 상태와 처리 대상을 유지. 책이 섞인 입력에서도 책당 목록 하나만 만들며 반환 순서는 원래 항목 순서 유지.
- 그룹 체크박스 키에 책 이름 해시를 사용하여 비슷하거나 긴 이름의 충돌 방지.
- 전체 선택/해제 버튼 사이 간격 14px(좁은/넓은 창 공통).
- 319개 테스트 통과. 그룹 선택·부분 해제·닫힌 목록 선택 유지·전체 선택/해제·유사 책 이름·영어 표시 추가 검사. compileall/diff 검사 통과. 실제 브라우저 클릭·픽셀 배치 검사는 미완료.
- v1.3.0 수정본 적용·재시작 완료. 설치 파일 46개 일치, 서버 health `ok`, 신규 오류 로그 0개. 직전 앱 백업: `backups/20260908-before-book-groups/MyBookshelf.app`.
